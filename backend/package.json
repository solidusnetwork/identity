{
  "name": "@solidus/identity-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && mkdir -p dist/db/migrations && cp src/db/migrations/*.sql dist/db/migrations/",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "migrate": "node dist/db/migrate.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/jwt": "^9.0.0",
    "@fastify/rate-limit": "^10.0.0",
    "@fastify/sensible": "^6.0.0",
    "@noble/ed25519": "^3.0.1",
    "@sentry/node": "^10.47.0",
    "@solidus/auth": "workspace:*",
    "@solidus/jwt": "workspace:*",
    "@solidus/sdk": "workspace:*",
    "@solidus/types": "workspace:*",
    "bullmq": "^5.0.0",
    "fastify": "^5.8.4",
    "ioredis": "^5.4.0",
    "postgres": "^3.4.4",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@solidus/config": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
